export enum QUERY_KEYS {
  SEARCH = 'keyword',
  KEY_PAGE_NUMBER = 'page_number',
  KEY_PAGE_SIZE = 'page_size',
}

export enum CUSTOM_EVENTS {
  SESSION_EXPIRE = 'SESSION_EXPIRE',
  UPDATE_INTERACTION_TIME = 'UPDATE_INTERACTION_TIME',
}
